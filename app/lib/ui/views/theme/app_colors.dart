import 'package:flutter/material.dart';

// Paleta base
const Color primaryColor = Color.fromRGBO(54, 86, 66, 1);
const Color backgroundColor = Color.fromRGBO(201, 217, 207, 1);

// Colores secundarios / auxiliares
const Color accentColor = Color(0xFF3D573B);
const Color dangerColor = Color(0xFFE57373);

// Colores del texto
const Color textPrimary = Color(0xFF333333);
const Color textSecondary = Color(0xFF666666);
